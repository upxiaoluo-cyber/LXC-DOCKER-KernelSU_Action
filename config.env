# ==============================================================================
# 内核源码配置（使用 Jujutsu-kernel 完整源码）
# ==============================================================================
# 内核源码地址（Iam-nishan 的 Jujutsu-kernel 完整源码仓库）
KERNEL_SOURCE=https://github.com/Iam-nishan/Jujutsu-kernel.git
# 内核源码分支（V6 分支包含 blossom_defconfig）
KERNEL_SOURCE_BRANCH=V6
# 内核编译配置（带 KernelSU 的配置，我们会替换为 SukiSU）
KERNEL_CONFIG=blossom_defconfig
# 内核Image.gz-dtb or Image
KERNEL_IMAGE_NAME=Image
ARCH=arm64
# 参数
EXTRA_CMDS:LD=ld.lld
EXTRA_CMDT:LLVM=1
EXTRA_CMDU:LLVM_IAS=1

# ==============================================================================
# 编译方案配置
# ==============================================================================
# 编译方案 'A'=clang+gcc编译 'B'=clang编译
METHOD_OK=A

# 编译器选择 'true'=使用自定义编译器Clang 'false'=使用AOSP编译器
USE_CUSTOM_CLANG=true

# Python切换（true=切换到python2）
CUT_PYTHON=false

# ==============================================================================
# 自定义编译器 Clang
# ==============================================================================
CUSTOM_CLANG_SOURCE=https://github.com/AOSP-12/prebuilts_clang_host_linux-x86_clang-r445002.git
CUSTOM_CLANG_BRANCH=12

# 如果使用自定义clang为false，则不要更改此项
CUSTOM_CMDS:CLANG_TRIPLE=aarch64-linux-gnu-

# ==============================================================================
# AOSP 编译器配置（仅供参考）
# ==============================================================================
CLANG_BRANCH=android-gs-bluejay-5.10-android13
CLANG_VERSION=clang-r416183b

# ==============================================================================
# LXC / Docker 配置
# ==============================================================================
LXC_DOCKER=false
LXC_PATCH=false
ANDROID_PARANOID_NETWORK_OFF=false

# ==============================================================================
# 🆕 SukiSU-Ultra 配置（替换 KernelSU）
# ==============================================================================
# 启用 SukiSU-Ultra
ENABLE_SUKISU=true

# SukiSU 版本（使用 main 分支）
SUKISU_VERSION=main

# SukiSU 官方仓库地址
SUKISU_SOURCE=https://github.com/SukiSU-Ultra/SukiSU-Ultra

# 集成方式: kprobe（推荐）
SUKISU_INTEGRATION_METHOD=kprobe

# ==============================================================================
# 兼容旧配置（如果 ENABLE_SUKISU 未生效，可降级到 KernelSU）
# ==============================================================================
ENABLE_KERNELSU=false
KERNELSU_TAG=v0.9.5

# ==============================================================================
# 额外内核配置
# ==============================================================================
DISABLE-LTO=false
DISABLE_CC_WERROR=false
ADD_KPROBES_CONFIG=false
ADD_OVERLAYFS_CONFIG=false

# ==============================================================================
# KVM 配置
# ==============================================================================
ENABLE_KVM=false

# ==============================================================================
# 缓存与输出配置
# ==============================================================================
ENABLE_CCACHE=true
NEED_DTBO=false
BUILD_BOOT_IMG=true
SOURCE_BOOT_IMAGE=https://github.com/upxiaoluo-cyber/LXC-DOCKER-KernelSU_Action/releases/download/1/boot.img
